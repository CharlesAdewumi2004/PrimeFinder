#ifndef PRIME_COUNTER_HPP
#define PRIME_COUNTER_HPP

#include <vector>

// Function prototype (declaration)
int count_primes_in_range(int start, int end);
std::vector<int> return_primes(int start, int end);

#endif // PRIME_COUNTER_HPP
